import React from "react";
import "./Sidebar.css"

const Sidebar = ({
    show,
    onClose,
}) => {
    return (
        <aside className={`sidebar`}>

        </aside>
    )
}

export default Sidebar;